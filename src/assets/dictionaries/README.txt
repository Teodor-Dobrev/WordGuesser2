Place your source dictionary files in this folder.

Required names:
- english.txt — one word per line using Latin letters.
- bulgarian.txt — one word per line using Cyrillic.

These files are embedded directly into the bundle, so updating them requires
rebuilding the app.
