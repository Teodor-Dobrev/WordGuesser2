Optional runtime overrides live here.

If you want to replace the built-in dictionaries without rebuilding the app,
add `english.txt` and/or `bulgarian.txt` in this folder. Each file should
contain one word per line.

When no override files are present the app falls back to the embedded
wordlists under `src/assets/dictionaries`.
